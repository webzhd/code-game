import React from 'react'
import {ItemType, RowProps} from './index'
function RowContent(props: RowProps) {
    // function getClassName(code: string, index: number): string {
    //     const valueIndex = targetCode.length - 1
    //     if(index > valueIndex) {
    //         return ''
    //     }
    //     if(code === targetCode[index]) {
    //         return styles.success
    //     }else{
    //         return styles.error
    //     }
    // }
    // return <>
    //     {
    //         props.textArray.map((item: ItemType, i: number) => {
    //             return [...item.text, ' '].map((code: string, index: number) => {
    //                 return <span key={`${i}-${index}`} className={getClassName(code, index)}>{code}</span>
    //             })
    //         })
    //     }
    // </>
}